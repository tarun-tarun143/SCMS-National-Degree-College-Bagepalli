import PortalShell from "@/components/portal/PortalShell"; import PageHeading from "@/components/portal/PageHeading"; import Badge from "@/components/ui/Badge";
export default function GenericPage({role,items,title,description,rows=["No records available yet."]}:{role:"student"|"faculty"|"admin";items:any[];title:string;description?:string;rows?:string[]}){return <PortalShell role={role} items={items} title={title}><PageHeading title={title} description={description}/><div className="grid gap-4">{rows.map((r,i)=><div key={i} className="card flex items-center justify-between p-5"><div><div className="font-bold text-[var(--navy)]">{r}</div><div className="mt-1 text-xs text-slate-500">SCMS module · configurable through the appropriate portal</div></div><Badge tone="blue">View</Badge></div>)}</div></PortalShell>}
