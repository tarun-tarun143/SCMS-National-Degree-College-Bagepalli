export default function PageHeading({eyebrow,title,description}:{eyebrow?:string;title:string;description?:string}){return <div className="mb-7"><div className="text-xs font-bold uppercase tracking-[.2em] text-[var(--blue)]">{eyebrow??"SCMS"}</div><h2 className="mt-1 text-2xl font-extrabold tracking-tight text-[var(--navy)] sm:text-3xl">{title}</h2>{description&&<p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">{description}</p>}</div>}
