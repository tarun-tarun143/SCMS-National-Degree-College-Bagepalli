export default function Progress({value,label}:{value:number;label?:string}){return <div><div className="mb-2 flex justify-between text-xs font-semibold text-slate-600">{label&&<span>{label}</span>}<span>{value}%</span></div><div className="h-2.5 rounded-full bg-slate-100"><div className="h-2.5 rounded-full bg-[var(--blue)]" style={{width:`${Math.max(0,Math.min(100,value))}%`}}/></div></div>}
