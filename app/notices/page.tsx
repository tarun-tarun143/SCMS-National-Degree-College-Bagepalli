"use client";
import PublicShell from "@/components/public/PublicShell"; import SectionTitle from "@/components/ui/SectionTitle"; import Badge from "@/components/ui/Badge"; import { firestoreDb } from "@/lib/firebase/client"; import { useLiveCollection } from "@/hooks/useLiveCollection";
type Notice={id:string;title?:string;category?:string;publishedAt?:string;date?:string;priority?:string;status?:string;description?:string};
export default function Notices(){const {data,loading,error}=useLiveCollection<Notice>(firestoreDb,"notices",{filters:[{field:"status",op:"==",value:"published"}],limit:100});return <PublicShell><main><section className="section-space"><div className="container-page"><SectionTitle eyebrow="Updates" title="Latest notices" description="Official announcements are streamed from the live college database."/><div className="mt-10 grid gap-4">{loading&&<div className="card p-6 text-sm text-slate-500">Loading notices…</div>}{error&&<div className="card bg-red-50 p-6 text-sm text-red-700">{error}</div>}{!loading&&!error&&!data.length&&<div className="card p-6 text-sm text-slate-500">No published notices are available right now.</div>}{data.map(n=><article className="card p-6" key={n.id}><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><Badge tone="blue">{n.category||"General"}</Badge>{n.priority&&n.priority!=="Normal"&&<Badge tone="amber">{n.priority}</Badge>}</div><h2 className="mt-3 text-lg font-extrabold text-[var(--navy)]">{n.title||"Untitled notice"}</h2><div className="mt-2 text-sm text-slate-500">Published {n.publishedAt||n.date||"Recently"}</div>{n.description&&<p className="mt-3 max-w-3xl text-sm leading-6 text-slate-600">{n.description}</p>}</div></div></article>)}</div></div></section></main></PublicShell>}